apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-postgis-sh
data:
  postgis.sh: |
    #!/bin/bash
    set -e

    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
        CREATE EXTENSION postgis SCHEMA public;
    EOSQL