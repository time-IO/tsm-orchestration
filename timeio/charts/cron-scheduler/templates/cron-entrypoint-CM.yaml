{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-entrypoint
data:
  cron-entrypoint.sh: {{ .Files.Get "conf/supercronic-entrypoint.sh" | quote }}
{{- end }}