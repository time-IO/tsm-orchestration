{{- if .Values.global.pvc.createPVC }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tomcat-context-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10M
  storageClassName: {{ .Values.global.pvc.storageClassName }}
{{- end }}