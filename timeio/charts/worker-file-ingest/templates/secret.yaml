{{- if .Values.existingSecret | empty }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.name }}-secret
type: Opaque
stringData:
  MQTT_USER: "{{  .Values.global.secrets.MQTT_USER }}"
  MQTT_PASSWORD: "{{  .Values.global.secrets.MQTT_PASSWORD }}"
  MINIO_ACCESS_KEY: "{{  .Values.global.secrets.MINIO_ROOT_USER }}"
  MINIO_SECURE_KEY: "{{  .Values.global.secrets.MINIO_ROOT_PASSWORD }}"
  S3MAP_DB_URL: "postgresql://{{  .Values.global.secrets.S3MAP_POSTGRES_USER }}:{{  .Values.global.secrets.S3MAP_POSTGRES_PASS }}@{{  .Values.global.envvars.S3MAP_POSTGRES_HOST }}/{{  .Values.global.envvars.S3MAP_POSTGRES_DB }}"
  SCHED_MQTT_USER: "{{  .Values.global.secrets.MQTT_USER }}"
  SCHED_MQTT_PASSWORD: "{{  .Values.global.secrets.MQTT_PASSWORD }}"
  SCHED_SLURM_USER: ""
  SCHED_SLURM_JWT_TOKEN: ""
{{- end }}