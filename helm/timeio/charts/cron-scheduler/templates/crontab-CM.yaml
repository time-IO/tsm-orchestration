{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-crontab
data:
  crontab.txt: |
    "###################################################"
    "# This is the crontab file for the cron-scheduler #"
    "###################################################"
    ""
{{- end }}